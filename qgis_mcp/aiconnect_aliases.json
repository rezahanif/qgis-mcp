{
 "_doc": "Authored intent phrasings, merged into aiconnect-capabilities.json by scripts/gen_capabilities.py. The AiConnect gateway folds these into its BM25 search haystack only - never into what the model is shown, and never into the embedding.",
 "_rule": "An alias must not introduce a word that is COMMON in ordinary English but RARE in the corpus. BM25 IDF treats such a word as maximally discriminating, so it becomes a magnet for queries the tool should refuse. Name the OPERATION - not a material, a trade, or any everyday noun. Authored from each tool's own name and description only, never from the benchmark task prompts.",
 "_why": "Driver: aiconnector/scripts/bundle-benchmark/alias_ab.py (BUNDLE_BENCH_TARGET=qgis).",
 "aliases": {
  "ping": ["is the qgis plugin server reachable", "connectivity check to qgis"],
  "diagnose": ["health of the qgis mcp stack", "plugin and server version match", "why is qgis not responding"],
  "list_qgis_instances": ["configured qgis endpoints", "which qgis instances are reachable"],
  "get_qgis_info": ["qgis version and profile path", "installed plugin count"],
  "get_project_info": ["project title and crs", "how many layers in the project", "project file metadata"],
  "load_project": ["open a qgs or qgz project", "load a saved qgis project"],
  "create_new_project": ["start a blank qgis project"],
  "save_project": ["save the qgis project", "write the project to a new path"],
  "get_layers": ["list layers in the project", "layer inventory with ids and types"],
  "add_vector_layer": ["load a shapefile", "add a geopackage or geojson layer", "bring vector data into the project"],
  "add_raster_layer": ["load a geotiff", "add raster imagery to the project", "bring a dem into the project"],
  "remove_layer": ["drop a layer from the project", "unload a layer by id"],
  "find_layer": ["locate layers by name pattern", "wildcard layer name search"],
  "create_memory_layer": ["scratch vector layer", "temporary in memory layer with fields"],
  "set_layer_visibility": ["show or hide a layer on the map", "toggle a layer in the tree"],
  "zoom_to_layer": ["zoom the canvas to a layer extent", "fit the map to a layer"],
  "get_layer_features": ["read attributes of features", "query features with an expression filter", "fetch rows from a vector layer"],
  "get_field_statistics": ["sum mean min max of a numeric field", "aggregate a column", "descriptive statistics for an attribute"],
  "add_features": ["insert new features with wkt geometry", "append records to a vector layer"],
  "update_features": ["edit feature attribute values", "change field values on features"],
  "delete_features": ["remove features by id or expression", "delete rows from a vector layer"],
  "update_feature_geometry": ["move or reshape feature geometry", "replace geometry with new wkt"],
  "start_editing": ["open an edit session on a layer", "begin editing a vector layer"],
  "commit_edits": ["save the edit buffer to the data source", "commit pending edits"],
  "rollback_edits": ["discard uncommitted edits", "revert changes on a layer"],
  "get_edit_status": ["is the layer being edited", "pending added and changed feature counts"],
  "undo_edits": ["step back an edit on a layer"],
  "redo_edits": ["reapply an undone edit"],
  "select_features": ["select features by expression", "highlight matching rows"],
  "get_selection": ["which features are selected", "current selection feature ids"],
  "clear_selection": ["deselect features on a layer"],
  "set_layer_style": ["categorized symbology by unique values", "graduated symbology over numeric ranges", "single symbol styling", "colour a layer by a field"],
  "set_raster_style": ["pseudocolor ramp on a raster band", "hillshade rendering", "rgb multiband rendering", "greyscale raster styling"],
  "get_canvas_extent": ["current map extent and crs"],
  "set_canvas_extent": ["pan and zoom the canvas to coordinates", "set the map bounding box"],
  "get_canvas_screenshot": ["fast grab of the map canvas widget", "quick canvas image without re-render"],
  "get_3d_screenshot": ["capture the open 3d map view", "image of the 3d scene"],
  "get_raster_info": ["raster band count and dimensions", "per band statistics and nodata", "raster extent and crs"],
  "execute_processing": ["run a processing algorithm", "buffer clip dissolve intersect", "invoke a native or gdal algorithm"],
  "list_processing_algorithms": ["search processing algorithms by keyword", "which algorithm does this"],
  "get_algorithm_help": ["parameters and outputs of an algorithm", "how to call a processing algorithm"],
  "create_processing_model": ["build a model3 graph from a spec", "chain algorithms into a reusable model"],
  "list_processing_models": ["registered processing models"],
  "run_model": ["execute a processing model by id", "run a model3 file"],
  "get_processing_providers": ["which providers are active", "gdal grass saga algorithm counts"],
  "execute_processing_batch": ["run one algorithm over many parameter sets", "batch processing over multiple inputs"],
  "raster_calculator": ["band math", "ndvi and normalized difference indices", "raster arithmetic expression"],
  "zonal_statistics": ["per polygon raster statistics", "summarise raster values inside polygons"],
  "sample_raster_values": ["pixel values at point coordinates", "extract raster values at locations"],
  "export_layer": ["write a layer to gpkg shp geojson or tif", "save a layer to disk with reprojection"],
  "field_calculator": ["populate a field from an expression", "compute a new attribute column"],
  "get_unique_values": ["distinct values of a field", "list the categories in a column"],
  "spatial_join": ["join attributes by location", "attach polygon attributes to points", "tag features by containment"],
  "render_map": ["render the map canvas to an image", "produce a rendered map picture"],
  "execute_code": ["run arbitrary pyqgis code", "script qgis directly with python"],
  "get_active_layer": ["which layer is selected in the panel"],
  "set_active_layer": ["make a layer the active one"],
  "get_canvas_scale": ["current map scale and rotation"],
  "set_canvas_scale": ["set the map scale denominator", "rotate the canvas"],
  "get_layer_labeling": ["current label configuration of a layer"],
  "set_layer_labeling": ["label features with a field", "turn labels on or off", "label font size and colour"],
  "get_layer_crs": ["coordinate reference system of a layer", "epsg code of a layer"],
  "set_layer_crs": ["reassign a layer crs without reprojecting", "fix a wrongly declared projection"],
  "get_bookmarks": ["saved spatial bookmarks"],
  "add_bookmark": ["save a map extent for quick navigation"],
  "remove_bookmark": ["delete a spatial bookmark"],
  "get_map_themes": ["saved visibility presets"],
  "add_map_theme": ["save the current layer visibility as a theme"],
  "remove_map_theme": ["delete a map theme"],
  "apply_map_theme": ["restore a saved visibility preset"],
  "set_project_crs": ["change the project coordinate reference system", "reproject the map canvas"],
  "batch_commands": ["several qgis commands in one round trip", "chain commands without repeated calls"],
  "list_layouts": ["print layouts in the project"],
  "export_layout": ["export a print layout to pdf or png", "render a layout at a given dpi"],
  "get_message_log": ["qgis log entries", "recent warnings and critical messages"],
  "list_plugins": ["installed qgis plugins and their state"],
  "get_plugin_info": ["details of an installed plugin"],
  "reload_plugin": ["reload a plugin during development"],
  "get_layer_tree": ["layer tree with groups and children", "structure of the layers panel"],
  "create_layer_group": ["group layers in the tree"],
  "move_layer_to_group": ["move a layer into a group"],
  "set_layer_property": ["layer opacity", "rename a layer", "scale dependent visibility"],
  "get_layer_extent": ["bounding box of a layer"],
  "get_project_variables": ["project level variables"],
  "set_project_variable": ["define a project variable for expressions"],
  "validate_expression": ["is this qgis expression valid", "check expression syntax and referenced columns"],
  "get_setting": ["read a qgis setting by key path"],
  "set_setting": ["write a qgis setting"],
  "transform_coordinates": ["reproject coordinates between crs", "convert a point or bbox to another projection"],
  "list_connections": ["saved data source connections", "browser panel entries for postgis and geopackage"],
  "create_postgresql_connection": ["save a postgis connection", "register a postgresql data source"],
  "list_connection_tables": ["tables and schemas behind a saved connection"],
  "add_layer_from_connection": ["load a database table as a layer", "query layer from sql against a connection"],
  "import_layer_to_connection": ["write a layer into postgis or geopackage", "publish a layer to a database table"],
  "execute_connection_sql": ["run sql server side on the database", "ddl or dml against a saved connection"],
  "add_web_layer": ["add an xyz basemap", "connect a wms or wfs service", "tile service layer"],
  "add_table_join": ["join a table to a vector layer by attribute"],
  "add_field": ["add an attribute column to a layer"],
  "delete_field": ["drop an attribute column"],
  "rename_field": ["rename an attribute column"],
  "apply_style_qml": ["load a qml style onto a layer"],
  "save_style_qml": ["export a layer style to qml"],
  "create_layout": ["new print layout"],
  "add_layout_map": ["place a map frame on a layout"],
  "get_layout_info": ["items and pages in a print layout"],
  "add_layout_label": ["place a text label on a layout", "dynamic expression text on a layout"],
  "add_layout_legend": ["place a legend on a layout"],
  "add_layout_scalebar": ["place a scale bar on a layout"],
  "add_layout_picture": ["place a north arrow or logo on a layout"],
  "add_layout_table": ["place an attribute table on a layout"],
  "configure_atlas": ["one layout page per feature", "atlas coverage layer and page naming"],
  "export_atlas": ["export atlas pages to pdf or images"],
  "remove_layout": ["delete a print layout"],
  "execute_sql": ["sql across loaded layers via a virtual layer", "join layers with a query"],
  "evaluate_expression": ["evaluate a standalone qgis expression", "aggregate over a layer in an expression"],
  "identify_features": ["what is at this coordinate", "map click identify across layers"],
  "duplicate_layer": ["copy a layer with its style"],
  "set_layer_order": ["reorder the draw order of layers", "move a layer above another"],
  "list_templates": ["ready to run pyqgis code recipes"],
  "load_template": ["load verified pyqgis template code by id"],
  "search_qgis_api": ["pyqgis class and method lookup", "qgis api documentation search"],
  "list_qgis_api_categories": ["browse pyqgis documentation categories"],
  "get_error_hints": ["recovery guidance for a typed qgis error code"],
  "qgis_function_registry_query": ["verified pyqgis call patterns", "has this pyqgis function been proven"],
  "register_verified_qgis": ["record a working pyqgis pattern in the registry"]
 }
}
