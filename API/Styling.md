<!-- generated by scripts/gen_api_docs.py from https://qgis.org/pyqgis/master/ — do not edit by hand -->

# QgsCategorizedSymbolRenderer

## Syntax
QgsCategorizedSymbolRenderer(attrName: str | None = '', categories: Iterable[QgsRendererCategory] = [])

## Description
The attrName argument specifies the layer’s field name, or expression, which the categories will be matched against. A list of renderer categories can optionally be specified. If no categories are specified in the constructor, they can be added later by calling addCategory().

```python
from qgis.core import QgsCategorizedSymbolRenderer
obj = QgsCategorizedSymbolRenderer(attrName, categories)
```

# QgsGraduatedSymbolRenderer.createRenderer

## Syntax
static createRenderer(vlayer: QgsVectorLayer | None, attrName: str | None, classes: int, mode: QgsGraduatedSymbolRenderer.Mode, symbol: QgsSymbol | None, ramp: QgsColorRamp | None, legendFormat: QgsRendererRangeLabelFormat = QgsRendererRangeLabelFormat(), useSymmetricMode: bool = False, symmetryPoint: float = 0, listForCboPrettyBreaks: Iterable[str | None] = [], astride: bool = False) -> QgsGraduatedSymbolRenderer | None

## Description
Creates a new graduated renderer. vlayer (Optional[QgsVectorLayer]) – vector layer attrName (Optional[str]) – attribute to classify classes (int) – number of classes mode (QgsGraduatedSymbolRenderer.Mode) – classification mode symbol (Optional[QgsSymbol]) – base symbol ramp (Optional[QgsColorRamp]) – color ramp for classes

```python
from qgis.core import QgsGraduatedSymbolRenderer
result = QgsGraduatedSymbolRenderer.createRenderer(vlayer, attrName, classes, mode, symbol, ramp, legendFormat, useSymmetricMode, symmetryPoint, listForCboPrettyBreaks, astride)
```

# QgsRendererCategory.setValue

## Syntax
setValue(self, value: Any)

## Description
Sets the value corresponding to this category. If value is a list, then the category will match any of the values from this list.

```python
from qgis.core import QgsRendererCategory
result = QgsRendererCategory.setValue(value)
```

# QgsSymbol.defaultSymbol

## Syntax
static defaultSymbol(geomType: Qgis.GeometryType) -> QgsSymbol | None

## Description
Returns a new default symbol for the specified geometry type. The caller takes ownership of the returned object.

```python
from qgis.core import QgsSymbol
result = QgsSymbol.defaultSymbol(geomType)
```

# QgsPalLayerSettings.setFormat

## Syntax
setFormat(self, format: QgsTextFormat)

## Description
Sets the label text formatting settings, e.g., font settings, buffer settings, etc. format (QgsTextFormat) – label text format

```python
from qgis.core import QgsPalLayerSettings
result = QgsPalLayerSettings.setFormat(format)
```

# QgsVectorLayerSimpleLabeling

## Syntax
QgsVectorLayerSimpleLabeling(settings: QgsPalLayerSettings)

## Description
Constructs simple labeling configuration with given initial settings

```python
from qgis.core import QgsVectorLayerSimpleLabeling
obj = QgsVectorLayerSimpleLabeling(settings)
```
