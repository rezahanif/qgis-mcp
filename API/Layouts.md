<!-- generated by scripts/gen_api_docs.py from https://qgis.org/pyqgis/master/ — do not edit by hand -->

# QgsLayoutExporter.exportToImage

## Syntax
exportToImage(self, filePath: str | None, settings: QgsLayoutExporter.ImageExportSettings) -> QgsLayoutExporter.ExportResult

## Description
Exports the layout to the filePath, using the specified export settings. If the layout is a multi-page layout, then filenames for each page will automatically be generated by appending “_1”, “_2”, etc to the image file’s base name. Returns a result code indicating whether the export was successful or an error was encountered. If an error code is returned, errorFile() can be called to determine the filename for the export which encountered the error.

```python
from qgis.core import QgsLayoutExporter
result = QgsLayoutExporter.exportToImage(filePath, settings)
```

# QgsLayoutExporter.exportToPdf

## Syntax
exportToPdf(self, filePath: str | None, settings: QgsLayoutExporter.PdfExportSettings) -> QgsLayoutExporter.ExportResult

## Description
Exports the layout as a PDF to the filePath, using the specified export settings. Returns a result code indicating whether the export was successful or an error was encountered. exportToPdf(iterator: Optional[QgsAbstractLayoutIterator], fileName: Optional[str], settings: QgsLayoutExporter.PdfExportSettings, feedback: Optional[QgsFeedback] = None) -> (QgsLayoutExporter.ExportResult, str) Exports a layout iterator to a single PDF file, with the specified export settings.

```python
from qgis.core import QgsLayoutExporter
result = QgsLayoutExporter.exportToPdf(filePath, settings)
```

# QgsLayoutItemMap.setExtent

## Syntax
setExtent(self, extent: QgsRectangle)

## Description
Sets a new extent for the map. This method may change the width or height of the map item to ensure that the extent exactly matches the specified extent, with no overlap or margin. This method implicitly alters the map scale.

```python
from qgis.core import QgsLayoutItemMap
result = QgsLayoutItemMap.setExtent(extent)
```

# QgsLayoutItemLegend.setLinkedMap

## Syntax
setLinkedMap(self, map: QgsLayoutItemMap | None)

## Description
Sets the map to associate with the legend.

```python
from qgis.core import QgsLayoutItemLegend
result = QgsLayoutItemLegend.setLinkedMap(map)
```

# QgsLayoutAtlas.setFilterFeatures

## Syntax
setFilterFeatures(self, filtered: bool)

## Description
Sets whether features should be filtered in the coverage layer.

```python
from qgis.core import QgsLayoutAtlas
result = QgsLayoutAtlas.setFilterFeatures(filtered)
```
